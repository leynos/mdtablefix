---
source: tests/cli.rs
expression: "fs::read_to_string(&in_place_path).expect(\"failed to read rewritten fixture\")"
---
# Output parity fixture

Before formatting, this prose introduces a representative document.

| Name  | Notes                              |
| ----- | ---------------------------------- |
| alpha | Use *`cargo test`* before merging. |

```rust
fn main() {
    println!("fixture");
}
```
